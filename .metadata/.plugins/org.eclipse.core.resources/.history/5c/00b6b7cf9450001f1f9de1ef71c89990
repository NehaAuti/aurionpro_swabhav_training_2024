package com.aurionpro.behavioural.observer.model;

public class AppNotifier implements INotifier {
    @Override
    public void notify(Account account) {
        System.out.println("App Notification: Account " + account.getAccountNumber() + " has been updated. Current balance: " + account.getBalance());
    }
}

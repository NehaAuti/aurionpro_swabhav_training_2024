package com.aurionpro.model;

import java.util.List;

import com.aurionpro.entity.Passbook;

public class TestPassbookDao {
    public static void main(String[] args) {
//        PassbookDao dao = new PassbookDao();
//        List<Passbook> entries = dao.getAllPassbookEntries();
//
//        if (entries.isEmpty()) {
//            System.out.println("No entries found in the passbook table.");
//        } else {
//            for (Passbook entry : entries) {
//                System.out.println(entry.getSenderAccno() + " -> " + entry.getReceiverAccno() + ": " + entry.getAmount());
//            }
//        }
    }
}